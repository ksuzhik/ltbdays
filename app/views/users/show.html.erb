<div class="row">
  <div class="span6 offset3 text-center">
      <h1><%="#{@user.first_name} #{@user.last_name}"%></h1>
      <h2><%=@user.birthdate.strftime('%d.%m.%Y')%></h2>
      <div class="span4 offset1"><%= image_tag(@user.avatar_url ? @user.avatar_url : 'no-image.jpg') %></div>
  </div>
</div>
